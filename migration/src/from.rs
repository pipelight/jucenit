use crate::Match;
use jucenit_core::{JuceConfig, JuceUnit};
use miette::{IntoDiagnostic, Result};

impl Match {
    pub fn from(juce_unit: &JuceUnit) -> Result<()> {
        // JuceUnit::from(value);
        Ok(())
    }
}
